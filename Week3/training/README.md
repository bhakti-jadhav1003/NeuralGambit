Training files

dataset.npz is not included because it is a generated dataset and exceeds GitHub's web upload limit.
Stockfish is an external dependency and should be downloaded separately before running the training scripts.
